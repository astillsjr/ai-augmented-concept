{
  "name": "nudgr-ai-augmentation",
  "version": "1.0.0",
  "description": "AI-augmented version of Nudgr’s NudgeEngine using Gemini API",
  "main": "dist/driver.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/driver.js",
    "dev": "ts-node --transpile-only src/driver.ts",
    "test": "npm run build && node dist/driver.js"
  },
  "keywords": ["gemini", "nudgr", "ai", "productivity", "typescript"],
  "author": "Anthony Stills",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
